# Outdated Data

Generated data is never repaired in place: when a generator or schema change leaves shipped datasets behind, the datasets are flagged here and regenerated from scratch with the current pipeline (see the no-repair rule in `AGENTS.md`). This file is maintained by hand — a session that notices outdated data adds an entry with the reason; whoever regenerates removes the entry once the fresh data is committed.

An entry names the affected data as precisely as known (a person id, a meta story id, or a corpus-wide condition), the generator change that outdated it, and what the reader loses until regeneration.

Hidden persons and collections are not tracked here. An entry the registries mark `"hidden": true` (`python scripts/set_hidden.py --list`) is off the deployed site, so nothing a reader sees depends on its state, and it is regenerated with the current pipeline before it is shown again. Every list and count below therefore covers the visible corpus only: 33 persons and 4 collections as of 2026-09-05. A session that shows a hidden entry again audits it against the conditions below and adds it where it meets one.

## Flagged for regeneration

- **All persons except `ada_lovelace`, `alan_turing`, `albert_einstein`, `alvar_aalto`, `charles_babbage`, `frances_allen`, `grace_hopper`, `niels_bohr`, `phillis_wheatley`, `zaha_hadid`** — the story predates the depth layer and the chapter art (as of 2026-09-05): events carry no `weight` (383 of 523 events), no event carries a `background` report, and chapters carry no `illustration` (129 of 174 chapters). The interface still derives a fallback weight, but the deep-event selection is weaker than the weighed one, `generate_event_backgrounds.py` refuses to fill reports for unweighted datasets, so those stories offer no way down on any event, and `ChapterSlide.svelte` opens every chapter on the headline alone, without the abstract image the slide is composed around. Regenerate with `scripts/generate_person.py`, which weighs the events in Phase 1, writes the reports after review, and runs the chapter art as Step 5 unless `--skip-chapter-art` is given. Of the nine filled persons, `ada_lovelace` predates the shorter report: its reports were written to a 350-550 word target that is now 250-350, so they read longer than anything the pipeline writes today. Length is not a loss to the reader, so this alone is not a reason to regenerate them.
- **`antoni_gaud`, `frei_otto`, `friedensreich_hundertwasser`, `heinz_nixdorf`, `joseph_weizenbaum`, `steve_jobs`** — the only `event_class` blocks are `birth` and `death`, the two the pipeline decides from the dates rather than from the model: every dataset here was last updated on or before 2025-12-20 and predates the `marriage_partnership`, `migration`, `invention`, and `publication` kinds. The classified slides those lives should have stay plain event badges — the Macintosh and the iPhone, Frei Otto's Munich Olympic roof, Weizenbaum's ELIZA paper. `frei_otto`, `heinz_nixdorf`, and `joseph_weizenbaum` additionally carry no `conclusion`, which Phase 1 writes as a required field: `ConclusionSlide.svelte` still prints its heading, its related persons, and its references, so the story closes on a headed box with no closing sentence. Regenerate with `scripts/generate_person.py`.
- **`albert_einstein`, `alexander_hamilton`, `benjamin_franklin`, `charles_babbage`, `david_hilbert`, `edward_bernays`, `erich_k_stner`, `frances_allen`, `franz_kafka`, `georg_wilhelm_friedrich_hegel`, `george_washington`, `hans_fallada`, `hedy_lamarr`, `john_von_neumann`, `phillis_wheatley`, `vannevar_bush`** — 30 publication `event_class` blocks carry a `source_link` (as of 2026-09-05; the German derivations carry the same), written by the publication-link step that the dataset generation dropped on 2026-09-05 together with the link on the slide. The interface no longer reads the field and `validate_source_links.py` no longer checks it, so the reader sees no difference; the field goes when a person is regenerated for any other reason with `scripts/generate_person.py`.
- **`antoni_gaud`, `herman_hollerith`** — three events hold their geocoded place only in the dead `location_coordinates` field and carry no `locations`, the field everything reads: Gaudí's fasting illness in Barcelona (1894), Hollerith's punched-card layout (1886), and his tabulation-system paper (1889). The review step that wrote the legacy field now writes `locations`, and the fallback through which `scripts/meta_story_map.py` could still have read the legacy list was dropped with issue #71 — no shipped collection map resolved through it, so nothing lost a pin, but a regenerated map will not place these three either, and the person story shows no place for them. The German derivations carry the same three. Regenerate with `scripts/generate_person.py`; the place comes back through the current pipeline, not through an edit of these files.
- **All persons except `alan_turing`, `alvar_aalto`, `grace_hopper`, `niels_bohr`, and `phillis_wheatley` (ego network)** — the network predates one or more of the schema changes of 2026-09-03 to 2026-09-05. Regenerate with `scripts/generate_person_network.py`; the German derivations carry every field and token named here. Each condition names the datasets that still meet it (as of 2026-09-05):
  - **Every network except `alan_turing`, `alvar_aalto`, `grace_hopper`, `niels_bohr`, and `phillis_wheatley`** carries `interaction_frequency` (`daily` through `rare`) on each of its connections (529 in all), a field the schema dropped on 2026-09-05: how often two people dealt with each other is rarely stated in the sources, so the value was the model's guess and could not be checked. The interface ignores the field, so the reader sees no difference on a chip.
  - **Every network except `alan_turing`, `alvar_aalto`, `charles_babbage`, `grace_hopper`, `niels_bohr`, and `phillis_wheatley`** carries `influence_direction`, `start_year`, `end_year`, and `shared_activities` on each connection (514 in all) and was generated under a prompt that admitted one-sided ties: someone the subject only read or admired, or who only later drew on the subject's work. The schema now defines a connection as a mutual tie — two people who dealt with each other directly — and carries none of the four fields. The interface ignores the fields, but the story no longer filters an event's chips by the tie's years, so a connection whose recorded years excluded an event now appears on it, and the networks still hold ties the current prompt would leave out of the graph. The `shared_activities` handling in `translate_person.py` can go once no dataset carries the key.
  - **`albert_einstein`, `edsger_w_dijkstra`, `frances_allen`, `joseph_weizenbaum`** carry nodes that are not individuals: 19 connections typed `entity_kind: organization` or `group` ("Nazi regime" as Einstein's `political/persecutor`, "IBM" and "National Security Agency" as Allen's employer and client, six faculty and working-group collectives in Dijkstra's network), plus two untyped collectives in `joseph_weizenbaum` ("General Electric Computer Development Laboratory colleagues", "Students at MIT and other universities"). The generator's schema and prompt now admit only named human beings and ask for the person through whom such a tie ran; the interface still renders the old nodes as labeled chips, but a network reader sees an institution or a crowd where every other node is a person. The `entity_kind` and `qualifier` handling in `PersonChip.svelte`, `translate_person.py`, and the locales can go once no dataset carries them.
  - **`albert_einstein`, `joseph_weizenbaum`, `konrad_zuse`**, and the meta stories **`computing_pioneers`** and **`organic_shapes_in_architecture`**, carry an `intellectual/…` relationship type — one connection each in the ego networks, three links in all in the meta stories, derived from those networks — a category the vocabulary has retired into `academic`: the two split one circle in two (Einstein's network opens an "Intellectual (1)" circle holding Bohr alone beside "Academic (4)"). The locale entry stays only until these are regenerated; regenerate the meta stories with `scripts/generate_meta_story.py` (Phase 5 + 5b + 6 for the network alone).
  - **`ada_lovelace`, `charles_babbage`, `steve_jobs`** — the first two label ordinary parents `family/biological_father`/`family/biological_mother` (issue #142): the closed vocabulary offered the token beside `father`/`mother` and asked for the most specific role, and every network generated under it took the qualified one. The birth card and the network view read "Biological Father" (German "Leiblicher Vater") on lives with no adoption in them. The generator now folds the qualifier away unless an adoptive or step parent stands in the same network; `steve_jobs` is that case and keeps it, but his adoptive parents carry the plain `father`/`mother` where the vocabulary now has `adoptive_father`/`adoptive_mother`.
- **`computing_pioneers`, `organic_shapes_in_architecture`, `quantum_physics_pioneers`** — regenerating member persons replaced the event identities behind the three meta stories and regenerated their ego networks. Incremental synchronization dropped the timeline references it could no longer match, so `quantum_physics_pioneers` opens its chapter `Quanta Enter the Atom (1905-1913)` on no events at all — Einstein's light-quantum work went with the Einstein regeneration, Bohr's `Publishes Three Atomic Model Papers` (1913-07) with the Bohr regeneration on 2026-09-05 — `computing_pioneers` lost its unmatched Ada Lovelace references and had its Babbage ones rewritten on 2026-09-03 and lost `Completed Turing machine paper` (1936-05-28) with the Turing regeneration on 2026-09-05, whose dataset dates the paper to its November 1936 publication as `Publishes On Computable Numbers`, then lost `Joins the National Physical Laboratory` (1945-10) with the Turing regeneration on 2026-09-06, whose dataset tells the NPL years as `Designs the Automatic Computing Engine`, and `organic_shapes_in_architecture` lost `Completes Viipuri Library` (1935) with the Aalto regeneration on 2026-09-05, whose dataset dates the library to its 1927 design, so the chapter `Modernism Meets the Body (1929-1943)` draws on two Aalto events instead of three, and it lost `Wins The Peak competition` (1983) and `MAXXI Museum and first Stirling Prize` (2010) with the Hadid regeneration on 2026-09-06, whose dataset drops the Peak competition and tells the Rome museum and the prize as two events. The three derived social networks still reflect the prior ego networks (`ada_lovelace`, `alan_turing`, `albert_einstein`, `alvar_aalto`, `charles_babbage`, `grace_hopper`, `niels_bohr`). All language copies are affected. Regenerate with `scripts/generate_meta_story.py`.
- **`abigail_adams`, `ada_lovelace`, `albert_einstein`, `alexander_hamilton`, `alvar_aalto`, `benjamin_franklin`, `charles_babbage`, `frances_allen`, `geoffrey_bawa`, `george_washington`, `grace_hopper`, `joseph_brant`, `niels_bohr`, `phillis_wheatley`, `satyendra_nath_bose` (annotations)** — every person generated from 2026-07-16 on was researched under a Phase 2 prompt that annotated on obscurity ("only annotate truly obscure terms", "DEFAULT to 0 annotations"), so a term a general reader cannot know from the sentence went unglossed when the model judged it standard in its field: the four persons regenerated on 2026-09-05 carry one annotated event among 61. Persons generated before July 2026 average ten annotated events of sixteen; these fifteen carry 45 among 234, and `alvar_aalto`, `niels_bohr`, `phillis_wheatley`, and `satyendra_nath_bose` have none. `alan_turing`, regenerated on 2026-09-06 under the current prompt, carries ten of sixteen. Since 2026-09-06 section 5 of `scripts/events/prompts/phase2.py` and the review pass in `scripts/utils/review_prompts.py` test whether an educated non-specialist would know the term instead. The reader taps nothing and gets no explanation. The German derivations carry the same gap. Regenerate with `scripts/generate_person.py`, or run `scripts/review_person.py`, which adds the missing annotations, marks the terms in the description, and keeps the annotations the event already has.
- **Corpus-wide: annotation explanations that restate the description** — the explanation says the sentence again: Turing's "Joins Bletchley Park" introduces the place in an appositive, "Bletchley Park, the wartime center of the British codebreaking organization", and its popup reads "Bletchley Park was the British wartime codebreaking center where the Government Code and Cypher School analyzed intercepted enemy communications." `python scripts/validate_event_prose.py` reports the restatements written in the slide's own words, 27 across 19 persons as of 2026-09-06 (visible: `alexander_hamilton`, `antoni_gaud`, `benjamin_franklin`, `charles_rennie_mackintosh`, `douglas_engelbart`, `edward_bernays`, `frank_lloyd_wright`, `frei_otto`, `hans_fallada`, `john_von_neumann`, `joseph_weizenbaum`, `steve_jobs`, `vannevar_bush`); a paraphrase escapes the count. The reader taps the term and learns nothing. Since 2026-09-06 Phase 2 is told that a sentence which introduces the term with an appositive has defined it, and the review pass removes a restating gloss via `dropped_annotations` and rewrites one that could go past the sentence, but the rule does not hold: `alan_turing`, regenerated from scratch on 2026-09-06 under exactly these prompts, carries five of the 27. Regeneration therefore does not clear this entry; the prompts or the review need the fix first.
- **`edsger_w_dijkstra` (annotations)** — one event glosses the subject of its own classification card: Dijkstra's event for his algorithm annotates the algorithm directly above an invention card headed with it that says the same thing. Phase 2 is told not to annotate the classified subject and did anyway; since 2026-09-06 `drop_classified_annotations` in `scripts/events/normalize.py` removes such a gloss at the normalize step and on review, and the shipped datasets predate the rule. The reader is offered the same explanation twice on one slide. The German derivations carry the same annotations. Regenerate with `scripts/generate_person.py`, or run `scripts/review_person.py`, whose save path applies the rule.
- **Corpus-wide: 41 annotations across 17 persons** (as of 2026-09-05) — the annotation explains a term that an earlier event of the same story already annotated or carried in its title: Babbage's "Lectures in Turin" and "Lovelace Publishes Engine Notes" both gloss the Analytical Engine two and three slides after "Designs Analytical Engine", Engelbart's and Bo Bardi's stories each carry six such repeats. Phase 2 researches each event in isolation, and `drop_repeated_annotations` in `scripts/events/normalize.py` now removes such repeats at the normalize step and on review; the shipped datasets predate the rule. The reader is offered a definition of what the previous slides were about. The German derivations carry the same annotations. Regenerate with `scripts/generate_person.py`, or run `scripts/review_person.py`, whose save path applies the rule.
- **`alan_turing` (event image), and every event image matched before 2026-09-06** — the slide for Turing's conviction (March 31, 1952) carries "Facade of Manchester County Court Offices", a photograph taken in 2016 of the court building that stands in Manchester today, which is neither the court that tried him nor a building that existed then. Phase 2 had written a search for a court in Manchester, the kind of place plus the city, and the matcher, which reads filenames and captions, took the name against "a court in Manchester" for the building where it happened; the photograph's date was in the Commons metadata and in none of the lines it was shown. `build_image_match_prompt` in `scripts/events/images/assign.py` now lists the year each photograph was taken, the shared stand-in list rejects a building found by its kind and its city, and Phase 2 is told to name a particular thing or leave the angle out. Every shipped image was matched without the year in view, so a present-day photograph of a place the event names only generically may sit on other slides too; the Turing slide is the one verified case. Nothing is broken — the image carries its license and credit — and it resolves when a person is regenerated for any other reason. Regenerate with `scripts/generate_person.py`.
- **Corpus-wide: 9 event images across 7 persons** (as of 2026-09-06: `abigail_adams`, `benjamin_franklin`, `george_washington`, `herman_hollerith`, `john_von_neumann`, `joseph_brant`, `konrad_zuse`) — the images are photographs of commemorations (a plaque, a grave, a statue, a memorial), which `score_commemoration` in `scripts/events/images/scoring.py` now penalizes so that a period photograph of the same subject outranks them; the shipped datasets were assembled while the penalty was unreachable and the score preferred the modern photograph. The reader is shown how a life is remembered where the story is telling the life: the plaque on von Neumann's birthplace on his birth, Hollerith's grave on his death. Nothing is broken — every image carries its license and credit — so this is a ranking loss rather than a defect, and it resolves when a person is regenerated for any other reason. Regenerate with `scripts/generate_person.py`.
- **Corpus-wide: every prose field written before 2026-09-05** — descriptions, conclusions, background reports, annotation explanations, relationship descriptions, category summaries, and the meta stories' article prose and cards were generated before the writing phases shared `scripts/utils/prose_style.py`, and they carry the constructions it now rules out: 20 of the 31 background reports contrast a fact against an alternative nobody proposed ("rather than", "not X but Y"), 25 of them carry a colon introducing an explanation, about one description in twelve closes on a sentence saying what the event "marked" or "reflected", and the German derivations mirror all of it ("statt", "nicht ... sondern"). Nothing is wrong on the slide, so this alone is not a reason to regenerate a person; it resolves when a person or meta story is regenerated for any other reason, and `scripts/review_person.py` now rewrites such sentences under the same rules for a person regenerated in part.
- **All persons (event prose)** — every description was written before the description contract (`scripts/utils/prose_style.py`, issue #141): Phase 1, Phase 2, and the review each carried their own partial style rules, the persona was a historian, and nothing read the output for register. `python scripts/validate_event_prose.py` lists the events where the reader sees it — 26 findings across 17 persons as of 2026-09-05: a later year in an event's own prose (Babbage's birth moving the family in 1808), a birthplace narrated as a source dispute (`charles_babbage`, `antoni_gaud`), a house number or street where the slide is at city level (`charles_babbage`, `charles_rennie_mackintosh`). `phillis_wheatley` and `alan_turing` were regenerated on 2026-09-05 and no longer appear. The rest of the corpus is not wrong, only written under the old rules; regenerate the persons the validator names first, with `scripts/generate_person.py`, and let its count fall to zero before `--check` gates CI.
- **Corpus-wide: 53 descriptions under twenty words across 25 persons, and the conclusions of `grace_hopper` and `marie_curie`** (as of 2026-09-06; `python scripts/validate_event_prose.py` lists them) — the prose was written while the prompts read their 2-4 sentence range as a ceiling and nothing as a floor: the shared prose block said the range was "not a length target", Phase 2 was told to refine a skeleton "shorter, never longer" although it holds more of the article than Phase 1, the review cut and never extended, and the class guidance kept the partner, the places, and the work out of the sentence because the card carries them. The datasets regenerated on 2026-09-05 (`alvar_aalto`, `grace_hopper`, `niels_bohr`, `phillis_wheatley`) carry most of the findings, with a median description of 21-26 words against the 60 of the first datasets: Grace Hopper's marriage reads "In 1930, she married a New York University professor." under a card naming Vincent Foster Hopper, and her conclusion is the one sentence "COBOL remains in use today in business and government computing." for a life the story spends sixteen slides on. The prompts now state the floor, let Phase 2 extend a thin skeleton from the article, keep the people and the place in the sentence, and ask the conclusion for 2-4 sentences, one per strand of the work that persists. The German derivations carry the same prose. Regenerate the four with `scripts/generate_person.py` first, or run `scripts/review_person.py`, whose prompt now extends a one-sentence description from the sources; the conclusion is written only by Phase 1, so it needs the full run.
- **`alexander_hamilton`, `edsger_w_dijkstra`, `frank_lloyd_wright`, `geoffrey_bawa`, `george_washington`, `herman_hollerith`, `john_von_neumann`, `konrad_zuse`, `phillis_wheatley`, `steve_jobs`, `vannevar_bush`** — a chapter holds a single event (as of 2026-09-06; `python scripts/validate_chapter_sizes.py` lists them, 13 chapters in all, two each in `edsger_w_dijkstra` and `george_washington`): Hamilton's `architect_of_american_finance` holds one event alone, so the chapter slide and the event slide that follows carry the same year, the same age, and the same place. Phase 1 asked for and checked only that every chapter holds an event; it now asks for at least two (`MIN_CHAPTER_EVENTS` in `scripts/events/pipeline.py`), refuses a plan below the floor, and asks the model once more with the reason. `phillis_wheatley` additionally carries a chapter that holds no event at all, `Death in Boston` (1784-12-05), while the death event sits in `Last Years`; the interface inserts a chapter slide only where an event names it, so the chapter and its illustration never render. The pipeline dated that chapter from an event, so the event left it after the dataset was written, on a path this audit did not find. The German derivations carry the same chapters. Regenerate with `scripts/generate_person.py`.
- **Corpus-wide: every event description** (as of 2026-09-06) — the descriptions were written and reviewed before the description contract (`scripts/utils/prose_style.py`) said that a description is read in sequence, and before the review (`scripts/review_person.py`) was asked to read the events in order and state, per event, what the slide adds, which terms it leans on unexplained, and which facts an earlier slide already told. Phase 2 refined each description with only its own event in view, so a slide can name a thing the story never introduced or tell again what the slide before it told. `python scripts/validate_event_prose.py` reports the detectable shape as "restates an earlier slide" (6 sentences across 4 persons on the current data: `albert_einstein`, `edward_bernays`, `frances_allen`, and `john_von_neumann`); the unexplained names it cannot detect. Until regeneration, or a `review_person.py --aspect events` run under the current prompt, a reader meets a slide that adds nothing to the one before it or leans on a name it was never given.
- **Corpus-wide: every birth and death description** (as of 2026-09-06) — the two boundary descriptions were written before `BIRTH_DESCRIPTION_GUIDANCE` and `DEATH_DESCRIPTION_GUIDANCE` in `scripts/events/event_classes.py` defined what their prose tells. The slide already shows the birth and the death themselves, the parents on the birth card and the cause on the death card, and many descriptions narrate that same fact once more: `douglas_engelbart` and `alvar_aalto` open on the birth and the parents alone, and `albert_einstein`, `david_hilbert`, and `alvar_aalto` close on the date and the age. The definition asks the birth for the household the child enters and grows up in and the death for what led to it and the conditions of the end, and Phase 2 may now rewrite such a skeleton longer, which it was forbidden for every other event. The German derivations carry the same prose. Nothing is wrong on the slide, so this alone is not a reason to regenerate a person; the description is rewritten when a person is regenerated for any other reason with `scripts/generate_person.py`, or by `scripts/review_person.py`, whose prompt now holds both descriptions to the definition.
- **Corpus-wide: 15 of 33 taglines** (as of 2026-09-05) — the tagline is built on a generic status noun (pioneer, architect, father, visionary, master, champion), mostly as "noun of ...": 'Pioneer of programmable computing', 'Architect of the Quantum Atom', 'Architect of relativity', 'Visionary of the Analytical Engine', six "Architect of ..." and eight "Pioneer of ..." in all. Phase 1 (`scripts/events/pipeline.py`) offered exactly these phrases as its examples, so the model reproduced the pattern, and on a card row the taglines stop telling the people apart. The prompt and the `PersonMetadata.tagline` schema description now ask for the specific work, idea, or discovery and rule the status-noun pattern out. Nothing is wrong on the card, so this alone is not a reason to regenerate a person; the tagline is rewritten when a person is regenerated for any other reason with `scripts/generate_person.py`, and the German copy follows through `scripts/translate_person.py`.
- **`ada_lovelace`, `alan_turing`, `konrad_zuse` (ego network), `quantum_physics_pioneers`** — prose carries Markdown emphasis (`*Childe Harold's Pilgrimage*` in a Lovelace background report, `**Maria Zuse**` in Zuse's category summaries, `*Mathematical Foundations of Quantum Mechanics*` in a theme connection of the meta story) that the interface renders verbatim as literal asterisks on the slide; `scripts/validate_markdown.py` lists the exact fields. The validator also reports one `images[].creator` string in `niels_bohr`, which is not prose: it is the Commons attribution for `File:Cernfounders.png`, copied verbatim with the literal asterisk the uploader wrote, and stays as the source states it. `alan_turing` carries an asterisk that is part of a machine's name: the Commons caption and source of `File:Manchester_Mk1*_TNMOC-CP.jpg` name the Manchester Mk1*, the successor to the Mark 1, so the validator reports a marker the source spells. `phillis_wheatley` carries the same shape in a `background_images[].caption` from the 2026-09-05 regeneration — the Commons description of `File:Washington's_headquarters_at_Cambridge_(NYPL_Hades-287428-EM3880).tiff` opens on a literal asterisk — so the depth layer shows it on the slide; the image step copies a Commons description into the caption without stripping the markers. The generation and translation prompts now forbid Markdown, so these datasets predate the fix: the German-only findings (parts of `ada_lovelace`) need only re-translation (`scripts/translate_person.py`), and the English ones need regeneration or review with the current prompts.
- **`frank_lloyd_wright` (German life events and ego network)** — both copies' stored `source_fingerprint` no longer matches their English source (`python scripts/translate_all_persons.py --target-lang de --check`). The German story and network prose therefore describe an English text that has since changed. Re-translate with `scripts/translate_person.py`.
- **German life events of `ada_lovelace`, `albert_einstein`, `alvar_aalto`, `charles_babbage`, `emmy_noether`, `frances_allen`, `franz_kafka`, `hedy_lamarr`, `max_planck`, `niels_bohr`, `otto_wagner`, and `phillis_wheatley`** — the 12 datasets that carry background-report figures and have not since been re-translated report stale (as of 2026-09-05) because the translation payload changed shape, not because the English text did. `scripts/translate_person.py` sent each event's figure captions as a second list of caption objects beside `images`, and the model read the two same-shaped arrays as one: it returned the report's captions inside `images`, left its own array empty, and the merge rejected the whole document over which array a caption sat in. The captions now travel as a flat `figure_captions` array of strings, which the fingerprint covers, so every German copy written under the old shape no longer matches its source. The German prose these files hold is the translation of the English text they still describe, so a reader sees nothing wrong; re-translate with `scripts/translate_person.py` to clear the flag.
