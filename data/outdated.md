# Outdated Data

Generated data is never repaired in place: when a generator or schema change leaves shipped datasets behind, the datasets are flagged here and regenerated from scratch with the current pipeline (see the no-repair rule in `AGENTS.md`). This file is maintained by hand — a session that notices outdated data adds an entry with the reason; whoever regenerates removes the entry once the fresh data is committed.

An entry names the affected data as precisely as known (a person id, a meta story id, or a corpus-wide condition), the generator change that outdated it, and what the reader loses until regeneration.

## Flagged for regeneration

- **All persons except `ada_lovelace`, `alan_turing`, `albert_einstein`, `charles_babbage`, `emmy_noether`, `frances_allen`, `franz_kafka`, `grace_hopper`, `hedy_lamarr`, `max_planck`, `otto_wagner`** — events carry no `weight` (656 of 829 corpus events, as of 2026-09-03): they predate Phase 1 weighting. The interface still derives a fallback weight, but the deep-event selection is weaker than the weighed one, and `generate_event_backgrounds.py` refuses to fill reports for unweighted datasets. Regenerate with `scripts/generate_person.py`.
- **Most persons** — events carry no `background` report where the story would offer a depth layer (only the eleven weighed persons above are filled): datasets predate the background-report step. Until regeneration, those stories offer no way down on the affected events; regeneration plus the pipeline's background step fills them. The seven filled before 2026-08-31 also predate the shorter report: they were written to a 350-550 word target that is now 250-350, so they read longer than anything the pipeline writes today. Length is not a loss to the reader, so this alone is not a reason to regenerate them.
- **All persons except `ada_lovelace`, `alan_turing`, `albert_einstein`, `charles_babbage`, `frances_allen`, `grace_hopper`, and `hedy_lamarr`** — chapters carry no `illustration` (239 of 275 corpus chapters, as of 2026-09-03): they predate the chapter-art step, which `generate_person.py` runs as Step 5 unless `--skip-chapter-art` is given. `ChapterSlide.svelte` opens those chapters on the headline alone, without the abstract image the slide is composed around. Four of the weighed persons — `emmy_noether`, `franz_kafka`, `max_planck`, and `otto_wagner` — were regenerated for their weights without the art, so weighting alone does not imply the illustrations are there. Regenerate with `scripts/generate_person.py`, or fill the art alone with `scripts/generate_chapter_illustrations.py`.
- **`antoni_gaud`, `cunigunde_of_luxembourg`, `e_t_a_hoffmann`, `frei_otto`, `friedensreich_hundertwasser`, `heinz_nixdorf`, `henry_ii`, `joseph_weizenbaum`, `levi_strauss`, `steve_jobs`, `umberto_eco`, `zaha_hadid`** — the only `event_class` blocks are `birth` and `death`, the two the pipeline decides from the dates rather than from the model: every dataset here was last updated on or before 2025-12-26 and predates the `marriage_partnership`, `migration`, `invention`, and `publication` kinds. The classified slides those lives should have stay plain event badges — the nine books Umberto Eco publishes across his story, the Macintosh and the iPhone, Levi Strauss's riveted-trouser patent, Henry II's marriage to Cunigunde — and a publication without the block also never reaches `enrich_publication_links.py`, so the work carries no link either. `friedensreich_hundertwasser`, `henry_ii`, and `levi_strauss` were added to this entry on 2026-09-03: they meet the condition and were missing from the list, not newly outdated. Regenerate with `scripts/generate_person.py`.
- **`cunigunde_of_luxembourg`, `frei_otto`, `heinz_nixdorf`, `joseph_weizenbaum`, `umberto_eco`, `zaha_hadid`** — `life_events.json` carries no `conclusion`, which the current chapter step writes as a required field. `ConclusionSlide.svelte` still prints its heading, its related persons, and its references, so the story closes on a headed box with no closing sentence. Regenerate with `scripts/generate_person.py`.
- **`albert_einstein`, `cunigunde_of_luxembourg`, `edsger_w_dijkstra`, `erich_k_stner`, `frances_allen`, `franz_kafka`, `joseph_weizenbaum`, `umberto_eco`, `zaha_hadid` (ego network)** — the network carries nodes that are not individuals: 38 connections typed `entity_kind: organization` or `group` ("Nazi regime" as Einstein's `political/persecutor`, "Gestapo", "IBM", "Neue Leipziger Zeitung editorial staff", eleven such nodes in Kästner's network alone), plus two untyped collectives in `joseph_weizenbaum` ("General Electric Computer Development Laboratory colleagues", "Students at MIT and other universities"). The generator's schema and prompt now admit only named human beings and ask for the person through whom such a tie ran; the interface still renders the old nodes as labeled chips, but a network reader sees an institution or a crowd where every other node is a person. The German derivations carry the same nodes. Regenerate with `scripts/generate_person_network.py`; the `entity_kind` and `qualifier` handling in `PersonChip.svelte`, `translate_person.py`, and the locales can go once no dataset carries them.
- **`albert_einstein`, `e_t_a_hoffmann`, `edward_bernays`, `joseph_weizenbaum`, `konrad_zuse` (ego network), and `computing_pioneers`, `organic_shapes_in_architecture` (meta story network)** — 19 connections carry an `intellectual/…` relationship type, a category the vocabulary has retired into `academic`: the two split one circle in two (Einstein's network opened an "Intellectual (1)" circle holding Bohr alone beside "Academic (4)"). The two meta stories carry the same tokens on the links derived from those networks. The locale entry stays only until these are regenerated. Regenerate the networks with `scripts/generate_person_network.py` and the meta stories with `scripts/generate_meta_story.py` (Phase 5 + 5b + 6 for the network alone).
- **All persons except `charles_babbage` and `grace_hopper` (ego network)** — every remaining connection in the corpus (963 as of 2026-09-03) carries `influence_direction`, `start_year`, `end_year`, and `shared_activities`, and every network was generated under a prompt that admitted one-sided ties: someone the subject only read or admired, or who only later drew on the subject's work. The schema now defines a connection as a mutual tie — two people who dealt with each other directly — and carries none of the four fields: a mutual tie has no direction of influence, the description dates the tie where the sources do, and the activity tags restated the description. The interface ignores the fields, so the reader sees no difference on a chip, but the story no longer filters an event's chips by the tie's years, so a connection whose recorded years excluded an event now appears on it, and the networks still hold ties the current prompt would leave out of the graph. The German derivations carry the same fields. Regenerate with `scripts/generate_person_network.py`; the `shared_activities` handling in `translate_person.py` can go once no dataset carries the key.
- **All persons (ego network)** — every connection in the corpus carries `interaction_frequency` (`daily` through `rare`), a field the schema dropped on 2026-09-05: how often two people dealt with each other is rarely stated in the sources, so the value was the model's guess and could not be checked. The interface no longer shows it and ignores the field, so the reader sees no difference on a chip; the datasets only carry a key the current pipeline does not write. The German derivations carry the same field. Regenerate with `scripts/generate_person_network.py`.
- **`e_t_a_hoffmann`, `erich_k_stner`, `franz_kafka` (ego network)** — the network carries a single `network_summary` where the current schema requires `category_summaries`, one per relationship circle. `NetworkModal.svelte` reads only the latter, so each circle in those three networks opens with no prose about what it meant. Regenerate with `scripts/generate_person_network.py`.
- **`antoni_gaud`, `claus_schenk_graf_von_stauffenberg`, `david_hilbert`, `hans_erlwein`, `hans_fallada`, `herman_hollerith`, `ida_noddack`** — ten events hold their geocoded place only in the dead `location_coordinates` field and carry no `locations`, the field everything reads. The review step that wrote the legacy field now writes `locations`, and the fallback through which `scripts/meta_story_map.py` could still have read the legacy list was dropped with issue #71 — no shipped collection map resolved through it, so nothing lost a pin, but a regenerated map will not place these ten either. They therefore show no place anywhere: Hilbert's marriage and his son's birth in Königsberg, Noddack's Berlin degree and rhenium sample, Hollerith's punched-card layout and tabulation paper, Gaudí's fasting illness in Barcelona, Stauffenberg's Berlin study, Erlwein's marriage, and Fallada's agricultural apprenticeship. The German derivations carry the same ten. Regenerate with `scripts/generate_person.py`; the place comes back through the current pipeline, not through an edit of these files.
- **`citizens_of_bamberg`** — two of the five chapters hold no events at all: `saintly_memory_and_dynastic_afterlife` (1025-1040), although Cunigunde's entry into Kaufungen and her death fall inside it, and `romantic_laboratory_of_the_arts_and_ideas` (1806-1816), whose whole cast is `e_t_a_hoffmann` and `georg_wilhelm_friedrich_hegel`. Both are declared members, both draw a lifeline on the timeline, and the composed prose names them, but neither contributes a single event — the same loss incremental synchronization caused in `computing_pioneers`. The story also carries no `geo_map`, the one collection of the five without a map section, though geography is what holds it together. Both language copies are affected. Regenerate with `scripts/generate_meta_story.py`.
- **`quantum_physics_pioneers`** — regenerating `albert_einstein` replaced the event identities behind the meta story. Incremental synchronization dropped its one unmatched Albert Einstein timeline reference, so the chapter `Quanta Enter the Atom (1905-1913)` now draws on Niels Bohr alone, without the light-quantum work it is named for. Both language copies are affected. Regenerate with `scripts/generate_meta_story.py`.
- **`computing_pioneers`** — regenerating `ada_lovelace`, and then `grace_hopper` and `charles_babbage` on 2026-09-03, replaced the event identities behind the meta story and regenerated both ego networks. Incremental synchronization removed the unmatched Ada Lovelace timeline references and rewrote the Babbage ones, while the derived social network still reflects the prior ego networks. Regenerate with `scripts/generate_meta_story.py`.
- **Corpus-wide: 30 event images across 22 persons** — the images are photographs of commemorations (a plaque, a grave, a statue, a memorial), which the candidate scoring now penalizes so that a period photograph of the same subject outranks them; the shipped datasets were assembled while the penalty was unreachable and the score preferred the modern photograph. The reader is shown how a life is remembered where the story is telling the life: Hilbert's tomb, Kafka's birthplace plaque. Regenerating `grace_hopper` on 2026-09-03 removed its share, including the Arlington grave the count was first written around; the corpus figure above is the 2026-08-31 measurement and has not been retaken. Nothing is broken — every image carries its license and credit — so this is a ranking loss rather than a defect, and it resolves when a person is regenerated for any other reason. Regenerate with `scripts/generate_person.py`.
- **Corpus-wide: every prose field written before 2026-09-05** — descriptions, conclusions, background reports, annotation explanations, relationship descriptions, category summaries, and the meta stories' article prose and cards were generated before the writing phases shared `scripts/utils/prose_style.py`, and they carry the constructions it now rules out: 77 of the 95 background reports contrast a fact against an alternative nobody proposed ("rather than", "not X but Y"), 84 of them carry a colon introducing an explanation, about one description in twelve closes on a sentence saying what the event "marked" or "reflected", and the German derivations mirror all of it ("statt", "nicht ... sondern"). Nothing is wrong on the slide, so this alone is not a reason to regenerate a person; it resolves when a person or meta story is regenerated for any other reason, and `scripts/review_person.py` now rewrites such sentences under the same rules for a person regenerated in part.
- **`frank_lloyd_wright` (German life events and ego network)** — both copies' stored `source_fingerprint` no longer matches their English source, the only person in the corpus that reports stale (`python scripts/translate_all_persons.py --target-lang de --check`). The German story and network prose therefore describe an English text that has since changed. Re-translate with `scripts/translate_person.py`.
- **`ada_lovelace`, `erich_k_stner`, `hedy_lamarr`, `konrad_zuse` (ego network), and `niels_bohr` (German only)** — prose carries Markdown emphasis (`*Childe Harold’s Pilgrimage*`, `**Maria Zuse**`) that the interface renders verbatim as literal asterisks on the slide; `scripts/validate_markdown.py` lists the exact fields. The generation and translation prompts now forbid Markdown, so most of these datasets predate the fix: the German-only findings (`niels_bohr`, parts of `ada_lovelace`) need only re-translation (`scripts/translate_person.py`), and the English ones need regeneration or review with the current prompts. `hedy_lamarr` was regenerated while the fix was landing — the run finished six minutes after the prompts changed, so it wrote its background report against the old ones — and needs the same regeneration or review.
