# The evaluation deployment: `vite --mode evaluation` and `vite build --mode
# evaluation` read this file and nothing else does. The one variable turns on
# the participant gate, the interaction logger, and the analysis page; see
# docs/agent/user-evaluation.md.
VITE_EVALUATION_MODE=1
