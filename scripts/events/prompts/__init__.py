"""What each call is told, kept apart from the code that makes the call.

One module per call that writes prose: `propose` proposes the events and
`research` researches one of them. The report
quotes these builders verbatim as the pipeline's prompt templates, so a change
here is a change to published documentation.
"""
