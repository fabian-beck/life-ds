# Python dependencies for data generation scripts
# openai>=2.0.0 is required for the modern client (OpenAI, APIStatusError)
# and structured outputs; the pre-1.0 SDK (0.x) is incompatible.
openai>=2.0.0
requests>=2.32.0
pydantic>=2.9.0
beautifulsoup4>=4.13.0
# Required by generate_person_portrait.py and convert_portraits_to_webp.py
pillow>=11.0.0
# Required by generate_report.py to compile docs/report/report.md
markdown>=3.7
