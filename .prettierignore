node_modules/
dist/
build/
.svelte-kit/
.venv/
.claude/
# Playwright writes JSON audits and reports here on every interface run.
test-results/
playwright-report/
data/persons.json
data/persons_*.json
data/person_styles.json
data/people/**/*.json
data/people/**/_cache/
# Written by the meta-story scripts through utils/json_io.py, in the same
# canonical format as the person data above. Prettier collapses a short array
# onto one line and the writer does not, so every composition, translation,
# and event sync left the tree failing format:check until someone ran Prettier
# by hand.
data/meta_stories.json
data/meta_stories_*.json
data/meta_story_styles.json
data/meta_stories/**/*.json
# Geocoder answers, written by the generation scripts and not committed.
data/_cache/
# Written by the fact-checking evaluation stages.
evaluation/out/
*.md
package-lock.json
pmtiles.exe
# Written by scripts/generate_report.py and record_pipeline_run.py. Only the
# generated files — docs/report/report.md is authored by hand.
docs/report/index.html
docs/report/summaries.json
docs/report/runs/
docs/report/screenshots/
# Vendored verbatim from protomaps.github.io by scripts/vendor_basemap_assets.mjs.
public/basemap-assets/
# Written by the dev server's emulation of the evaluation log API.
.evaluation-logs/
